;; (ns var-tool.server.utils
;;   (:gen-class)
;;   (:require [clojure.string :as string]))
;;
;; (defn build-path
;;   "Build a file-path with slashes from a path-collection."
;;   [col]
;;   (string/join "/" col))
